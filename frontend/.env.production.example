# =============================================================
#  MitraFinance Frontend — PRODUCTION Environment Template
#  These variables are inlined into the JS bundle at build time.
#  Rebuild & redeploy the frontend whenever any value changes.
# =============================================================

# ── Backend API ───────────────────────────────────────────────
VITE_API_URL=https://api.mitrafinance.com/api

# ── API Key ───────────────────────────────────────────────────
# Must exactly match API_KEY in the backend .env.
# IMPORTANT: This value will be visible in the compiled JS bundle.
# It is a client-identification header, NOT a password.
# Rotate it immediately if the bundle is ever publicly exposed or leaked.
VITE_API_KEY=CHANGE_ME_same_value_as_backend_API_KEY
